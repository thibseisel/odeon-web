/**
 * The format of inputs accepted by a Pie Chart (`ngx-charts-pie-chart`).
 */
export type PieSeries = Array<{ 
    name: string, 
    value: number 
}>

/**
 * The format of inputs accepted by a Bar Chart (`ngx-charts-bar-vertical`).
 */
export type BarSeries = Array<{
    name: string,
    value: number
}>